export interface User {
  name: string;
  email: string;
  avatar: string;
  gender: string;
  _id: string;
  theme: string;
  dueDate: string;
}
export interface LoginData {
  email: string;
  password: string;
}